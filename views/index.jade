extends layout

block content
	h1 Strength History
	p A web interface will be available eventually.
	- if (user)
		p You are #{user.username}!
			br
			a(href='/logout') Logout
		select(id='select')
			option Save
			option Remove
			option Update
		br
		input(id='id', type='text', name='id', value='someid')
		br
		input(id='time', type='text', name='time', value='1374951455047')
		br
		input(id='weight', type='text', name='weight', value='79.5')
		br
		input(id='send', type='button', value='Send')
		br
	- else
		- if (message)
			p #{message}
		form(action='/login', method='post')
			input(id='username', type='text', name='username', value='test')
			br
			input(id='password', type='password', name='password', value='test')
			br
			input(type='submit', value='Login')
	iframe(src='/test')
